## What this changes

A sentence or two on what is different and why.

## Why

The problem it solves, or the issue it closes.

## How it was verified

What you ran, and what it printed. "Tests pass" on its own is not verification -
say which tests, and add one that fails without the change where that makes sense.

```
```

## Checklist

- [ ] `pytest` passes
- [ ] `ruff check` is clean
- [ ] Documentation updated if behaviour changed
- [ ] No secrets, credentials, real host addresses, or customer data in the diff
